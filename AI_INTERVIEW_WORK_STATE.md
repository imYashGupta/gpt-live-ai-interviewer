# AI interview integration: resume checkpoint

Updated: 2026-09-23. Read NEXT_TASK_HANDOFF.md and plan section 18 for implementation details.

## Objective and completed work
- Durable deletion implemented across service and ATS: tenant authorization, tombstones, immediate service revocation, copy erasure, retryable cleanup, active-provider stop/unknown-outcome handling, stale work fencing and numeric audit preservation.
- ATS supports provisioned-interview deletion, signed deletion events and polling recovery, application deletion integration, and deletion while the pilot flag is disabled. Unresolved create mappings must be reconciled before deletion.
- Automatic retention remains disabled. No period selected; no claim of provider storage, backups, sent mail or downloaded-copy purge.

## Branches / working trees
- Both remain `codex/ai-interview-integration`. Resumed from service `4ac5ddc` / ATS `7d9031833`; completed ATS deletion workflow committed as `e06dd0e2f`. Service implementation/checkpoints committed under `feat(interview-service): add durable interview deletion`; resolve its final hash with git log.
- Preserve service untracked `tsconfig.tsbuildinfo` and ATS modified `composer.lock`.

## Verification / operations
- Service: 33 database tests on disposable Herd schemas, 59 unit/contract/transport tests (3 DB suites skipped by the unit command), lint and non-incremental TypeScript passed. Mocked providers only.
- ATS: 44 focused integration/client/status tests / 315 assertions and the existing nested application/human-interview deletion test / 9 assertions passed (45 tests / 324 assertions total). Pint passed. Recruiter HTTP deletion is covered.
- Applied additive service migration 003 and ATS migration 2026_09_19_170401 to existing local Herd databases. No existing interview was deleted.
- Earlier Next and dedicated ATS worker PIDs exited outside this task (cause not inferred). Restored Next HTTPS (PIDs 22633/22634) in exec session 73305 using existing Herd certificates. Authenticated capabilities: 200/schema 1.0; unknown deletion status: 404; no active/provisional live attempts.
- 2026-09-21: that Next HTTPS dev server had wedged (PID 22634 spinning ~100% CPU, no listening socket, no response on https://127.0.0.1:3000) while still holding the `.next/dev` lock, so new `next dev` runs exited 1 with "Another next dev server is already running". Sent SIGTERM to 22633/22634; both exited and port 3000 is free. Not restarted at user request. The stale `.next/dev/lock` file is harmless (OS advisory lock, already released). Superseded on the same day: local HTTPS no longer uses Next's `--experimental-https` at all (see the Herd proxy entry below).
- No service worker, ATS worker or scheduler was started. No paid provider session, invitation, billing, push, deployment or production config change. Exact-host allowlisting and public IPv4 HTTPS/443 production policy unchanged.

## Pending / exact next safe action
- 2026-09-23 implemented: service side committed and fast-forwarded into service `main` (push of `main` to origin requested by user on 2026-09-23; verify with `git ls-remote origin main`); Recooty side still UNCOMMITTED on its branch and excluded from the merge by user choice: retail billing + BETA_FEATURES gate (plan section 19). Service: `billableSeconds` policy + `INTERVIEW_BILLABLE_ACCOUNT_IDS` (completed/cancelled bill measured seconds capped at duration; interrupted/failed/fake/deleted bill 0). Recooty: `AiInterviewBilling` (AI_INTERVIEW_SECONDS string feature, team-locked holds on create, charge once on newly stored usage, release on terminal/failure/deletion, negative adjustment refund ticket, retry re-hold), `INTERVIEW_SERVICE_BILLING` flag, gate switched from `INTERVIEW_SERVICE_TEAM_IDS` to BETA_FEATURES, panel shows available/held/charged time, migration `2026_09_23_120000_add_billing_state_to_ai_interviews` (also inserts the feature row).
- Checks: service 60 unit/contract/transport pass (3 DB suites skipped by unit command) + 34/34 Herd DB tests, ESLint and non-incremental tsc clean. Recooty: 36 integration + 14 unit client/status tests pass, 69 incl. custom-webhook suite, 40 related interview/application deletion/scheduling tests, Pint clean, repo tsc at 1112 baseline with no panel errors. Mocked providers only.
- NOT done (needs operator decision): Recooty migration not applied locally; local team 1 (Starter plan) lacks BETA_FEATURES so the panel will be hidden for it until granted; `INTERVIEW_SERVICE_TEAM_IDS` in local ATS `.env` is now unused. Plan allowances/add-on prices for AI_INTERVIEW_SECONDS not set. recooty/core enum case deferred.
- 2026-09-21: local HTTPS moved off `next dev --experimental-https` onto a Herd nginx proxy site. Created `herd proxy ai-interviewer http://127.0.0.1:3000 --secure`, so `https://ai-interviewer.test` terminates TLS in nginx and forwards to Next over loopback HTTP. Changed the origin from `https://interview-bot.test:3000` to `https://ai-interviewer.test` in service `.env.local` (`INTERVIEW_SERVICE_ORIGIN`), ATS `/Users/yashgupta/Laravel/recooty/.env` (`INTERVIEW_SERVICE_BASE_URL`, backup `.env.bak.herd-proxy`, no Laravel config cache to clear) and `next.config.ts` (`allowedDevOrigins`). `interview-bot.test` was left alone: it belongs to the unrelated Laravel app at /Users/yashgupta/Laravel/interview-bot/ and still answers 200.
- Observed after the move: `npm run build` succeeded (11 static pages, 7 dynamic routes, middleware bundled). `next start --hostname 127.0.0.1 --port 3000` is listening (background PID 44466, started from this session; it is not a service and will not survive a reboot). Through the proxy: `https://ai-interviewer.test/` 307 to `/access?next=%2F` (passcode gate), `http://` 301 to HTTPS, `/v1/capabilities` 401 without credentials, and Herd PHP reached the same URL with TLS verification passing (curl errno 0, HTTP 401), so the ATS trust path works. No authenticated ATS call, worker, provider session or billing operation was run.
- 2026-09-21 consumer-side audit of /Users/yashgupta/Laravel/recooty after the origin change: no hardcoded service origin in ATS code (only `config/interview-service.php` -> `INTERVIEW_SERVICE_BASE_URL`). Authenticated `capabilities` over the new origin returned 200 / schema 1.0 with TLS verified by Herd PHP; workspace mapping team 1 -> ws_cb5e0353 intact. Persisted links: ATS `ai_interviews.access_url` is encrypted at rest, 3 of 12 rows still carry `https://interview-bot.test:3000`, all expired 2026-09-19/20; service `service_access_links` stores only token hashes plus expiry and mints URLs from the origin at issue time, 16 links total, 0 live or unclaimed. No working candidate link was invalidated by the rename. Inbound direction untouched: webhook endpoint we_adb84aa5 -> recooty.test still active and the POST route is registered. Pre-existing backlog unrelated to this change (all 2026-09-18): 10 failed `SyncAiInterview` jobs, latest 'Usage identity mismatch', and 1 of 9 usage-inbox rows unprocessed with `mapping_pending` - these are the already-tracked unknown-usage reconciliation gap. Event inbox fully processed; 0 queued jobs. Audit was read-only apart from the GET capabilities probe.
- 2026-09-21 runtime: ATS interview 978db191 was stuck at exec=queued only because no Laravel queue worker was running. QUEUE_CONNECTION=redis, so the `jobs` table read 0 while the job sat in Redis queue `{ai-interview-pilot}`; check queue depth with `Queue::connection('redis')->size(...)`, not the table. A bounded `queue:work redis --queue={ai-interview-pilot} --stop-when-empty --tries=1` provisioned it: service_interview_id int_0a6c7f3b, exec=ready, access link minted on the new origin as `https://ai-interviewer.test/join` expiring 2026-09-23.
- Then started both workers in this session (background, not services, will not survive a reboot): ATS `queue:work redis --queue={ai-interview-pilot},{ai} --tries=1` (PID 66661) and service `npm run service:worker` (PID 66760, reports customer billing disabled). A real candidate session followed on the new origin: attempt att_19398de0, provider openai_live, consented, observer ready, started 16:13:13 IST and ended 16:13:58 IST (~45s), connection_state closed. Final ATS state exec=completed, assess=insufficient_evidence (expected for a 45s session), usage=settled; 0 failed jobs today, Redis queue drained, all service jobs done including `deliver`, which confirms signed webhook delivery to recooty.test still works after the origin change. Full provision -> live -> assess -> settle -> deliver path is now verified end to end against `https://ai-interviewer.test`.
- 2026-09-21 later: all three session-owned processes (Next `next start` PID 44466, ATS queue worker 66661, service worker 66760) were stopped. The stop was reported by the harness as a kill; the cause was not observed and is not inferred. Verified afterwards: no matching processes remain, port 3000 is free, `https://ai-interviewer.test` returns 502 from nginx (proxy site intact, upstream absent). Nothing was left mid-flight: service_jobs has 0 non-done rows and 0 stale leases, service_attempts has 0 rows with a null ended_at, the ATS Redis queue total (ready+delayed+reserved) is 0, 0 failed jobs today and 0 unprocessed inbox rows. Interview 978db191 stayed terminal at exec=completed / assess=insufficient_evidence / usage=settled. No restart was performed; the operator must relaunch `next start`, `queue:work` and `npm run service:worker` before the next interview.
- All required checks and grouped local commits completed. ATS has only unrelated composer.lock modified; service has only unrelated tsconfig.tsbuildinfo untracked. This final checkpoint observation is included by amending the same service commit. No push or pending runtime mutation.
- Both checkpoint files and plan section 18 updated with inventory, workflow, observed migrations/tests and remaining policy gates.
- Next safe action: review/commit the Recooty section 19 billing work, then (with approval) apply the Recooty migration locally and grant BETA_FEATURES + AI_INTERVIEW_SECONDS to the pilot team. Earlier: decide the next lifecycle policy/acceptance slice using plan section 18. Runtime workers remain stopped intentionally; reinspect matching processes and active/pending work before starting any worker.
- Broader remaining policy/acceptance: unresolved provider creations, unknown usage reconciliation, team hard-deletion restrictions, unprovisioned-create deletion, retention policy and backup restore drills; public production webhook acceptance remains deferred.
